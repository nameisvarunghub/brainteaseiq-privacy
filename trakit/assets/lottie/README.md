# Lottie

Drop animated JSON files here for richer microinteractions:

- `confetti.json` — fire after the first successful screenshot scan.
- `magic_wand.json` — Quick-add AI parsing flourish.
- `empty_wallet.json` — empty-state for Timeline before any capture.

Use `lottie` (already in `pubspec.yaml`) to render them.
